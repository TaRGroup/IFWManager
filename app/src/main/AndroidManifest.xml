<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="trumeet.IfwManager">
    <uses-permission android:name="android.permission.ACCESS_SUPER_USER" />
    <uses-permission android:name="trumeet.IfwManagerKey.permissions.UNLOCK" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:icon="@mipmap/ic_launcher">
        <activity android:name=".ui.activities.XmlListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.SettingsActivity"
            android:label="@string/settings_title" />
        <activity android:name=".ui.activities.XmlDetailTabActivity" />
    </application>
</manifest>
